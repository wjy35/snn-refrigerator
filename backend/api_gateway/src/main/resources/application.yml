spring:
  cloud:
    gateway:
      routes:
        - id: member-example
          uri: lb://MEMBER-EXAMPLE/
          predicates:
            - Path=/member-example/**
          filters:
            - RewritePath=/member-example/(?<path>.*),/$\{path}
