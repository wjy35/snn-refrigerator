spring:
  cloud:
    gateway:
      routes:
        - id: member-example
          uri: lb://MEMBER-EXAMPLE/
          predicates:
            - Path=/member-example/**
          filters:
            - RewritePath=/member-example/(?<path>.*),/$\{path}
        - id: recipe
          uri: lb://RECIPE/
          predicates:
            - Path=/recipe/**
          filters:
            - RewritePath=/recipe/(?<path>.*),/$\{path}

